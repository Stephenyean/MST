#ifndef HEAD_H
#define HEAD_H
#include <vector>
using namespace std;
vector<float> generatePoints(int L, int H, int Number);
vector<float> generatePoints(string filename);
void processData(string);
void processData2(string);
#endif
